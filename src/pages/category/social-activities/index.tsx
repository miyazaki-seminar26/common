import { Text } from '@mantine/core';
import type { NextPage } from 'next';

import { AppLayoutWithHeader } from '@/components/layouts/AppLayoutWithHeader';

const SocialActivities: NextPage = () => {
  return (
    <AppLayoutWithHeader>
      <Text>SocialActivities</Text>
    </AppLayoutWithHeader>
  );
};
export default SocialActivities;
